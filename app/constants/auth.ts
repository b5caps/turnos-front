// Temporal: el backend pide usuarioId y todavía no hay login real.
// El número sale del seed del back (pnpm prisma db seed).
// Borrar junto con este archivo cuando exista autenticación.
export const USUARIO_DEMO = 1
